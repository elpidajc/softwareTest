# 计算器模块需求文档

## 功能需求
1. 支持整数和浮点数的加法、减法、乘法、除法。
2. 除法操作需处理除零错误，返回 "Error: Division by zero"。
3. 所有运算结果保留两位小数（如适用）。
4. 输入非数字时应抛出 `ValueError` 并提示 "Invalid input"。

## 非功能需求
- 代码需有单元测试覆盖所有功能。
- 测试报告需自动生成并在 VSCode 中预览。
- 使用 Git 管理版本，每个功能提交对应一个 Issue。